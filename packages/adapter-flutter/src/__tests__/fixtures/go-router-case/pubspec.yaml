name: go_router_fixture
description: A fixture Flutter app using go_router.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.0

flutter:
  uses-material-design: true
